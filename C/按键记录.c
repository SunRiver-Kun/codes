#include<conio.h>
if(_kbhit())    //是否按了键盘，返回0,1
{
    fflush(stdin);  //清空缓冲区的字符
    key=getch();    //第一次返回0~224
    key=getch();    //第二次返回真实值
}
/*
	*控制台按键所代表的数字
	*“↑”：72
	*“↓”：80
	*“←”：75
	*“→”：77
	*/
